apiVersion: apps/v1
kind: Deployment
metadata:
  name: twins-service
  labels: { app: twins-service, part-of: xappx }
spec:
  replicas: 2
  selector:
    matchLabels: { app: twins-service }
  template:
    metadata:
      labels: { app: twins-service }
    spec:
      containers:
        - name: twins-service
          image: ghcr.io/xappx/twins-service:latest
          ports:
            - containerPort: 8085
          env:
            - name: DATABASE_URL
              valueFrom: { secretKeyRef: { name: twins-service-db, key: url } }
            - name: BROKER_URL
              valueFrom: { secretKeyRef: { name: xappx-broker, key: url } }
            - name: SERVICE_NAME
              value: twins-service
          readinessProbe:
            httpGet: { path: /readyz, port: 8085 }
            initialDelaySeconds: 5
          livenessProbe:
            httpGet: { path: /healthz, port: 8085 }
            initialDelaySeconds: 15
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 500m, memory: 512Mi }
---
apiVersion: v1
kind: Service
metadata:
  name: twins-service
spec:
  selector: { app: twins-service }
  ports:
    - port: 80
      targetPort: 8085
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: twins-service
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: twins-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: 70 }
