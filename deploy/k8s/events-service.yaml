apiVersion: apps/v1
kind: Deployment
metadata:
  name: events-service
  labels: { app: events-service, part-of: xappx }
spec:
  replicas: 2
  selector:
    matchLabels: { app: events-service }
  template:
    metadata:
      labels: { app: events-service }
    spec:
      containers:
        - name: events-service
          image: ghcr.io/xappx/events-service:latest
          ports:
            - containerPort: 8093
          env:
            - name: DATABASE_URL
              valueFrom: { secretKeyRef: { name: events-service-db, key: url } }
            - name: BROKER_URL
              valueFrom: { secretKeyRef: { name: xappx-broker, key: url } }
            - name: SERVICE_NAME
              value: events-service
          readinessProbe:
            httpGet: { path: /readyz, port: 8093 }
            initialDelaySeconds: 5
          livenessProbe:
            httpGet: { path: /healthz, port: 8093 }
            initialDelaySeconds: 15
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 500m, memory: 512Mi }
---
apiVersion: v1
kind: Service
metadata:
  name: events-service
spec:
  selector: { app: events-service }
  ports:
    - port: 80
      targetPort: 8093
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: events-service
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: events-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: 70 }
