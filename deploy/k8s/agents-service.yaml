apiVersion: apps/v1
kind: Deployment
metadata:
  name: agents-service
  labels: { app: agents-service, part-of: xappx }
spec:
  replicas: 2
  selector:
    matchLabels: { app: agents-service }
  template:
    metadata:
      labels: { app: agents-service }
    spec:
      containers:
        - name: agents-service
          image: ghcr.io/xappx/agents-service:latest
          ports:
            - containerPort: 8086
          env:
            - name: DATABASE_URL
              valueFrom: { secretKeyRef: { name: agents-service-db, key: url } }
            - name: BROKER_URL
              valueFrom: { secretKeyRef: { name: xappx-broker, key: url } }
            - name: SERVICE_NAME
              value: agents-service
          readinessProbe:
            httpGet: { path: /readyz, port: 8086 }
            initialDelaySeconds: 5
          livenessProbe:
            httpGet: { path: /healthz, port: 8086 }
            initialDelaySeconds: 15
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 500m, memory: 512Mi }
---
apiVersion: v1
kind: Service
metadata:
  name: agents-service
spec:
  selector: { app: agents-service }
  ports:
    - port: 80
      targetPort: 8086
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: agents-service
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: agents-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: 70 }
