apiVersion: apps/v1
kind: Deployment
metadata:
  name: export-service
  labels: { app: export-service, part-of: xappx }
spec:
  replicas: 2
  selector:
    matchLabels: { app: export-service }
  template:
    metadata:
      labels: { app: export-service }
    spec:
      containers:
        - name: export-service
          image: ghcr.io/xappx/export-service:latest
          ports:
            - containerPort: 8094
          env:
            - name: DATABASE_URL
              valueFrom: { secretKeyRef: { name: export-service-db, key: url } }
            - name: BROKER_URL
              valueFrom: { secretKeyRef: { name: xappx-broker, key: url } }
            - name: SERVICE_NAME
              value: export-service
          readinessProbe:
            httpGet: { path: /readyz, port: 8094 }
            initialDelaySeconds: 5
          livenessProbe:
            httpGet: { path: /healthz, port: 8094 }
            initialDelaySeconds: 15
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 500m, memory: 512Mi }
---
apiVersion: v1
kind: Service
metadata:
  name: export-service
spec:
  selector: { app: export-service }
  ports:
    - port: 80
      targetPort: 8094
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: export-service
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: export-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: 70 }
