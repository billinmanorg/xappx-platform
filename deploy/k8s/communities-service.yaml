apiVersion: apps/v1
kind: Deployment
metadata:
  name: communities-service
  labels: { app: communities-service, part-of: xappx }
spec:
  replicas: 2
  selector:
    matchLabels: { app: communities-service }
  template:
    metadata:
      labels: { app: communities-service }
    spec:
      containers:
        - name: communities-service
          image: ghcr.io/xappx/communities-service:latest
          ports:
            - containerPort: 8088
          env:
            - name: DATABASE_URL
              valueFrom: { secretKeyRef: { name: communities-service-db, key: url } }
            - name: BROKER_URL
              valueFrom: { secretKeyRef: { name: xappx-broker, key: url } }
            - name: SERVICE_NAME
              value: communities-service
          readinessProbe:
            httpGet: { path: /readyz, port: 8088 }
            initialDelaySeconds: 5
          livenessProbe:
            httpGet: { path: /healthz, port: 8088 }
            initialDelaySeconds: 15
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 500m, memory: 512Mi }
---
apiVersion: v1
kind: Service
metadata:
  name: communities-service
spec:
  selector: { app: communities-service }
  ports:
    - port: 80
      targetPort: 8088
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: communities-service
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: communities-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: 70 }
