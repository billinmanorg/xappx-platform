apiVersion: apps/v1
kind: Deployment
metadata:
  name: campaigns-service
  labels: { app: campaigns-service, part-of: xappx }
spec:
  replicas: 2
  selector:
    matchLabels: { app: campaigns-service }
  template:
    metadata:
      labels: { app: campaigns-service }
    spec:
      containers:
        - name: campaigns-service
          image: ghcr.io/xappx/campaigns-service:latest
          ports:
            - containerPort: 8089
          env:
            - name: DATABASE_URL
              valueFrom: { secretKeyRef: { name: campaigns-service-db, key: url } }
            - name: BROKER_URL
              valueFrom: { secretKeyRef: { name: xappx-broker, key: url } }
            - name: SERVICE_NAME
              value: campaigns-service
          readinessProbe:
            httpGet: { path: /readyz, port: 8089 }
            initialDelaySeconds: 5
          livenessProbe:
            httpGet: { path: /healthz, port: 8089 }
            initialDelaySeconds: 15
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 500m, memory: 512Mi }
---
apiVersion: v1
kind: Service
metadata:
  name: campaigns-service
spec:
  selector: { app: campaigns-service }
  ports:
    - port: 80
      targetPort: 8089
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: campaigns-service
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: campaigns-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: 70 }
