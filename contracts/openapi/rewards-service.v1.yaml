openapi: 3.1.0
info:
  title: XAPPX rewards-service
  version: 1.0.0
  description: Action catalogue, rules engine, and the double-entry points ledger.
servers:
- url: https://api.xappx.com/api/v1
paths:
  /healthz:
    get:
      summary: Liveness
      responses:
        '200':
          description: OK
  /readyz:
    get:
      summary: "Readiness \u2014 includes broker and database"
      responses:
        '200':
          description: OK
        '503':
          description: Not ready
components:
  schemas:
    Problem:
      $ref: ../schemas/problem.schema.json
