openapi: 3.1.0
info:
  title: XAPPX identity-service
  version: 1.0.0
  description: Global user identity, per-brand membership, roles and permissions.
servers:
- url: https://api.xappx.com/api/v1
paths:
  /healthz:
    get:
      summary: Liveness
      responses:
        '200':
          description: OK
  /readyz:
    get:
      summary: "Readiness \u2014 includes broker and database"
      responses:
        '200':
          description: OK
        '503':
          description: Not ready
components:
  schemas:
    Problem:
      $ref: ../schemas/problem.schema.json
